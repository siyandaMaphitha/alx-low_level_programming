#include "main.h"

/**
 * _abs - computes the absloute of an interger
 * @z: the int to check
 * Return: the absolute value of int
 */

int _abs(int z)
{
	if (z >= 0)
		return (z);
	else
		return (-z);
}
